{
  "name": "grantsetu-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that fronts grantsetu.in and serves a branded fallback page (with email capture) when the home-server origin is offline.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.90.0"
  }
}
